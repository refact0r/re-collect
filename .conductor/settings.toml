"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "pnpm install; cp $CONDUCTOR_ROOT_PATH/.env.local .env.local"

[scripts.run.run]
available_in = "local"
command = "pnpm dev:all"
default = true
icon = "play"
